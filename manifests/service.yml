apiVersion: v1
kind: Service
metadata:
    name: novatecstackakswithdevopsandterraform
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: novatecstackakswithdevopsandterraform